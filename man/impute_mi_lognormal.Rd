% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_mi.R
\name{impute_mi_lognormal}
\alias{impute_mi_lognormal}
\title{Multiple imputation of below-detection values from a censored lognormal}
\usage{
impute_mi_lognormal(
  x,
  lod,
  censored = is.na(x),
  m = 20,
  seed = NULL,
  param_uncertainty = TRUE
)
}
\arguments{
\item{x}{Numeric vector of concentrations. Censored entries may be
\code{NA}/\code{NaN}.}

\item{lod}{Numeric limit of detection, length 1 or \code{length(x)}.}

\item{censored}{Logical vector marking left-censored observations. Defaults
to \code{is.na(x)}.}

\item{m}{Number of completed datasets to return (default 20).}

\item{seed}{Optional integer; sets the RNG for reproducibility.}

\item{param_uncertainty}{If \code{TRUE} (default), draw parameters per imputation
for proper MI; if \code{FALSE}, condition on the MLE (improper MI).}
}
\value{
A list of length \code{m}, each element a numeric vector the length of
\code{x} with censored entries imputed and detected entries unchanged.
}
\description{
Fits a left-censored lognormal by maximum likelihood and draws \code{m} completed
datasets. Each completion (i) optionally draws fresh parameters
\eqn{(\mu^*, \log\sigma^*)} from their asymptotic sampling distribution
(Rubin's proper multiple imputation, propagating estimation uncertainty),
then (ii) replaces each censored observation with a draw from the fitted
normal truncated above at \code{log(lod)}, back-transformed to the concentration
scale. Detected observations are carried through unchanged.
}
\details{
Analyse the \code{m} completions separately and combine with Rubin's rules to
obtain inferences that reflect the missing-below-LOD information.
}
\examples{
set.seed(1)
x <- rlnorm(50, 0, 1)
lod <- 0.5
cens <- x < lod
x[cens] <- NA
imps <- impute_mi_lognormal(x, lod = lod, censored = cens, m = 5, seed = 1)
length(imps)
}
\references{
Rubin, D. B. (1987). \emph{Multiple Imputation for Nonresponse in
Surveys}. Wiley.
}
\seealso{
\code{\link[=impute_mle]{impute_mle()}}, \code{\link[=impute_ros]{impute_ros()}}
}
