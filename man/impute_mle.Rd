% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_mle.R
\name{impute_mle}
\alias{impute_mle}
\title{Impute below-detection values by censored-lognormal conditional means}
\usage{
impute_mle(x, lod, censored = is.na(x))
}
\arguments{
\item{x}{Numeric vector of concentrations (must be positive where
detected). Censored entries may be \code{NA}/\code{NaN}.}

\item{lod}{Numeric limit of detection, length 1 or \code{length(x)}.}

\item{censored}{Logical vector marking left-censored observations. Defaults
to \code{is.na(x)}.}
}
\value{
A numeric vector the length of \code{x}, censored entries replaced by
their conditional means (capped below \code{lod}), detected entries unchanged.
}
\description{
Fits a left-censored lognormal by maximum likelihood (internal helper
\code{.fit_censored_lognormal()}) and replaces each censored observation
with its model-based conditional expectation \eqn{E[X \mid X < L]}, where
\eqn{L} is that observation's limit of detection. This is a deterministic,
parametric single imputation: it uses all of the data (detected and
censored) to estimate the distribution, but like any single imputation it
does not propagate estimation or imputation uncertainty (use
\code{\link[=impute_mi_lognormal]{impute_mi_lognormal()}} for that).
}
\details{
For \eqn{Y = \log X \sim N(\mu, \sigma^2)} and \eqn{c = \log L},
\deqn{E[X \mid X < L] = e^{\mu + \sigma^2/2}\,
  \frac{\Phi((c - \mu - \sigma^2)/\sigma)}{\Phi((c - \mu)/\sigma)}.}
}
\examples{
set.seed(1)
x <- rlnorm(50, 0, 1)
lod <- 0.5
cens <- x < lod
x[cens] <- NA
head(impute_mle(x, lod = lod, censored = cens))
}
\references{
Helsel, D. R. (2012). \emph{Statistics for Censored Environmental Data Using
Minitab and R} (2nd ed.). Wiley.
}
\seealso{
\code{\link[=impute_mi_lognormal]{impute_mi_lognormal()}}, \code{\link[=impute_ros]{impute_ros()}}
}
