% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_ros.R
\name{impute_ros}
\alias{impute_ros}
\title{Impute below-detection values by regression on order statistics (ROS)}
\usage{
impute_ros(x, lod, censored = is.na(x))
}
\arguments{
\item{x}{Numeric vector of concentrations (positive where detected).
Censored entries may be \code{NA}/\code{NaN}.}

\item{lod}{Numeric limit of detection, length 1 or \code{length(x)}. All censored
observations must share one limit.}

\item{censored}{Logical vector marking left-censored observations. Defaults
to \code{is.na(x)}.}
}
\value{
A numeric vector the length of \code{x}. Censored entries are replaced by
ROS-fitted values (each below the detection limit), returned in ascending
order across the censored positions; detected entries are unchanged.
}
\description{
Semiparametric lognormal ROS for a single detection limit. Detected values
are assigned probability plotting positions in the upper \eqn{1 - p_e}
fraction of the distribution (where \eqn{p_e} is the censored proportion),
a least-squares line is fitted to \code{log(detected)} against the normal
quantiles of those positions, and the censored observations are filled from
the fitted line at plotting positions spread through the lower \eqn{p_e}
fraction. Unlike substitution, ROS uses the shape of the detected data to
reconstruct a plausible left tail, which typically improves estimates of
the mean and standard deviation.
}
\details{
This implementation assumes a \strong{single} detection limit among the censored
observations. Apply it per assay or analyte (where the limit is constant);
supplying multiple distinct limits is an error. Fully multiply-censored ROS
(Helsel and Cohn, 1988) is not yet implemented.
}
\examples{
set.seed(1)
x <- rlnorm(50, 0, 1)
lod <- 0.5
cens <- x < lod
x[cens] <- NA
head(impute_ros(x, lod = lod, censored = cens))
}
\references{
Helsel, D. R. and Cohn, T. A. (1988). Estimation of descriptive
statistics for multiply censored water quality data. \emph{Water Resources
Research}, 24(12), 1997-2004.
}
\seealso{
\code{\link[=impute_mle]{impute_mle()}}, \code{\link[=impute_mi_lognormal]{impute_mi_lognormal()}}
}
