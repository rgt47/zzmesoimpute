% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_substitution.R
\name{impute_substitution}
\alias{impute_substitution}
\title{Impute below-detection values by simple substitution}
\usage{
impute_substitution(x, lod, censored = is.na(x), fraction = 0.5)
}
\arguments{
\item{x}{Numeric vector of measured concentrations. Censored entries may be
\code{NA}/\code{NaN}; their value is ignored and replaced.}

\item{lod}{Numeric limit of detection, either a single value or one value
per element of \code{x}. Must be finite and positive at censored positions.}

\item{censored}{Logical vector marking left-censored (below-LOD)
observations. Defaults to \code{is.na(x)}.}

\item{fraction}{Single number in \verb{(0, 1]} multiplied by \code{lod}. Defaults to
\code{0.5} (LOD/2).}
}
\value{
A numeric vector the same length as \code{x}, with censored entries
replaced by \code{lod * fraction} and observed entries unchanged.
}
\description{
Replaces each left-censored observation with a fixed fraction of the limit
of detection, \code{lod * fraction}. Common choices are \code{fraction = 1/2}
(LOD/2) and \code{fraction = 1/sqrt(2)} (LOD/sqrt(2); Hornung and Reed, 1990).
Substitution is deterministic and simple but biases variance estimates and
any quantity sensitive to the sub-LOD distribution.
}
\examples{
x <- c(5.2, NA, 3.1, NA)
impute_substitution(x, lod = 1.5)
impute_substitution(x, lod = 1.5, fraction = 1 / sqrt(2))
}
