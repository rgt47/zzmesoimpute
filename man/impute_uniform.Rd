% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute_uniform.R
\name{impute_uniform}
\alias{impute_uniform}
\title{Impute below-detection values by a uniform draw on (0, LOD)}
\usage{
impute_uniform(x, lod, censored = is.na(x), seed = NULL)
}
\arguments{
\item{x}{Numeric vector of measured concentrations. Censored entries may be
\code{NA}/\code{NaN}; their value is ignored and replaced.}

\item{lod}{Numeric limit of detection, either a single value or one value
per element of \code{x}. Must be finite and positive at censored positions.}

\item{censored}{Logical vector marking left-censored (below-LOD)
observations. Defaults to \code{is.na(x)}; for MSD data supply the explicit
below-range indicator instead.}

\item{seed}{Optional integer. When supplied, \code{set.seed(seed)} is called
before drawing so the imputation is reproducible.}
}
\value{
A numeric vector the same length as \code{x}, with censored entries
replaced by uniform draws and observed entries unchanged.
}
\description{
Replaces each left-censored observation with an independent draw from a
Uniform(0, \code{lod}) distribution. This is the incumbent Meso Scale Discovery
(MSD) lab method (a single random imputation); it injects sub-threshold
variability but does not propagate imputation uncertainty and assumes a
uniform sub-LOD distribution. Provided primarily as a benchmark comparator.
}
\examples{
x <- c(5.2, NA, 3.1, NA)
impute_uniform(x, lod = 1.5, seed = 1)
}
