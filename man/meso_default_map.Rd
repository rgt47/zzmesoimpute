% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meso_default_map.R
\name{meso_default_map}
\alias{meso_default_map}
\title{Default Meso Scale Discovery column map}
\usage{
meso_default_map()
}
\value{
A named character vector, canonical name = header regex.
}
\description{
The mapping used by \code{\link[=read_meso]{read_meso()}} to rename Meso Scale Discovery (MSD)
export columns to canonical snake_case names. It is a named character
vector whose \strong{names are the canonical output names} and whose \strong{values
are case-insensitive regular expressions} matched against the trimmed
header labels of the export.
}
\details{
The patterns are written to absorb the labelling differences seen across
MSD software versions, e.g. \verb{Calc. Conc} vs \verb{Calc. Concentration},
\verb{Calc. Low} vs \verb{Detection Limits: Calc. Low}, and \verb{Standard Conc} vs
\verb{Standard Concentration}. Each pattern must match at most one header in a
given file (an ambiguous match is an error).

To support a non-standard export, copy this vector and override or add
entries, e.g.
\code{read_meso(path, col_map = c(meso_default_map(), my_field = "^My Label$"))}.
Use \code{\link[=meso_headers]{meso_headers()}} to inspect a file's actual labels first.
}
\examples{
meso_default_map()[c("calc_conc", "calc_low")]
}
\seealso{
\code{\link[=read_meso]{read_meso()}}, \code{\link[=meso_headers]{meso_headers()}}, \code{\link[=meso_required_cols]{meso_required_cols()}}
}
