% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_meso.R
\name{read_meso}
\alias{read_meso}
\title{Read a Meso Scale Discovery export into a canonical table}
\usage{
read_meso(
  path,
  sheet = 1,
  col_map = meso_default_map(),
  header_marker = "Plate Name",
  na = c("NaN", "..", ""),
  required = meso_required_cols(),
  keep_unmapped = FALSE
)
}
\arguments{
\item{path}{Path to the \code{.xlsx} export.}

\item{sheet}{Sheet name or index (default 1).}

\item{col_map}{Named character vector, canonical name = header regex. See
\code{\link[=meso_default_map]{meso_default_map()}}. Override or extend it for non-standard exports.}

\item{header_marker}{Label identifying the header row (default
\code{"Plate Name"}).}

\item{na}{Strings to treat as missing (default \code{c("NaN", "..", "")}).}

\item{required}{Canonical names that must be present; an error is raised if
any is missing. Default \code{\link[=meso_required_cols]{meso_required_cols()}}. Pass \code{character(0)} to
disable the check.}

\item{keep_unmapped}{If \code{TRUE}, columns not matched by \code{col_map} are
retained under their original labels. Default \code{FALSE} (they are dropped).}
}
\value{
A data frame with one row per measurement and canonical columns.
Concentration-like columns are coerced to numeric; identifier columns to
character.
}
\description{
Generic ingest for Meso Scale Discovery (MSD) immunoassay exports. The
header row is found by locating the \code{header_marker} cell (rather than a
fixed \code{skip}), and columns are renamed to canonical snake_case names via a
user-overridable \code{col_map}. This handles MSD layouts that differ in the
number of preamble rows, in label wording, and in the number of trailing
columns (e.g. an appended \verb{\% Recovery} / \verb{\% Recovery Mean}).
}
\seealso{
\code{\link[=meso_default_map]{meso_default_map()}}, \code{\link[=meso_headers]{meso_headers()}}, \code{\link[=meso_required_cols]{meso_required_cols()}}
}
